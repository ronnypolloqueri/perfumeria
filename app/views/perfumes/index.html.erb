<table class="table table-striped">
  <thead>
    <tr>
      <th><center>Imagen</center></th>
      <th><center>Nombre</center></th>
      <th><center>Marca</center></th>
      <th><center>Sexo</center></th>
      <th><center>Tipo</center></th>
      <th><center>Mililitro</center></th>
      <th><center>Precio</center></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @perfumes.each do |perfume| %>
      <tr>
        <td><center><%= image_tag(perfume.imagen_path, class: "img-rounded",width: "30%")%></center></td>
        <td><%= perfume.nombre %></td>
        <td><%= perfume.marca.descripcion %></td>
        <td><%= perfume.sexo.descripcion %></td>
        <td><%= perfume.tipo.descripcion %></td>
        <td><%= perfume.mililitro.descripcion %></td>
        <td><%= perfume.precio %></td>
        <td><%= link_to 'Mostrar', perfume %></td>
        <td><%= link_to 'Editar', edit_perfume_path(perfume) %></td>
        <td><%= link_to 'Eliminar', perfume, method: :delete, data: { confirm: '¿Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Perfume', new_perfume_path,  class: "btn btn-primary" %>
